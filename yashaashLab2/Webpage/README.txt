This is an angular app using d3 AngularWordCloud.
We have displayed the module according to their data collection
source in the form of a dropdown.
Each element is clickable which will give you the word count
for that element.

Command:
to run the app, go inside the dicwordcloud directory in cmd
and type "ng serve".
The app will open up in http://localhost:4200/